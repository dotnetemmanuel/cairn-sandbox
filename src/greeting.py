def hello(name):
    return f"Greetings, {name}!"


def loud(message):
    return message.upper() + "***"


def quiet(message):
    return message.lower() + "###"
    return message.lower() + "~~~"
    return message.lower() + " (resolved)"
