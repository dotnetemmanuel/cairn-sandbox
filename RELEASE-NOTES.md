# Release notes

## Unreleased
- Trunk advanced after the feat-* stack branched off, so the stack is now a
  few commits behind `main`. Run `git town sync --stack` (or `S` → sync in
  Cairn) to rebase the whole stack onto the new trunk.
